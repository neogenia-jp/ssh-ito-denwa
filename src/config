#####
####
###   ssh-ito-denwa 設定ファイル
##
#


# SSH 接続先ホスト
SSH_HOST=

# SSH 接続先ポート番号（デフォルト22）
#SSH_PORT=

# ポートフォワードの設定
#   リモートポート:転送先ホスト:転送先ポート
REV_PORT_FORWARD=28086:localhost:8082


# --- 以下は、パスワード認証を使う場合 ---
# 鍵認証を使う場合は、
# root ユーザの ssh_config /root/.ssh/config に接続設定を書き、
# ホスト名だけを指定して接続可能な状態にしておけばOKです。
# ---
#SSH_PASS=
#USER_NAME=

